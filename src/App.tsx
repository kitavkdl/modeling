// 옛 씬은 Task 5에서 src/engine/scene으로 옮겨갔다. 엔진 씬은 AssemblyProvider가 있어야
// 돌아가는데 옛 App에는 프로바이더가 없다. Task 6(프로바이더)·Task 8(앱 교체)까지
// 화면을 비워 두고, 남은 옛 파일은 타입 검사만 통과시킨다.
export default function App() {
  return null
}
